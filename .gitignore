# ── Python: virtualenv e bytecode (creati da start.sh fase 3) ──
.venv/
venv/
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ── Generati al primo avvio (start.sh) ──
# binario encdec compilato con il prefisso segreto dentro
.encdec/
# config del wizard: contiene claude_api_key / serper_api_key
config.json

# ── Supabase: dati runtime del Docker stack ──
supabase/volumes/db/data

# ── Macchine Docker di test (create dal wizard) ──
docker-test-machine-linux
docker-test-machine-windows

# ── Tooling locale ──
.claude
*.log
*.pid

# ── UI build (Tailwind) ──
# node_modules is dev-only; static/vendor/tailwind.css is committed (served
# fallback when npm is unavailable) so it is intentionally NOT ignored.
node_modules/

# ── Segreti runtime: MAI committare ──
.vfa_auth_secret
assets.txt.migrated
assets.txt

# ── AI role testing: MAI committare ──
role_test_report_v1.0.56-beta.xlsx
.~lock.role_test_report_v1.0.56-beta.xlsx#