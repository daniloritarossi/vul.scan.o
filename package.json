{
  "name": "vulnerability-feed-aggregator-ui",
  "version": "1.0.0",
  "private": true,
  "description": "Tailwind build for the VUL.SCAN.O UI (replaces the in-browser Play CDN runtime).",
  "scripts": {
    "css": "tailwindcss -i ./static/src/app.css -o ./static/vendor/tailwind.css --minify",
    "css:watch": "tailwindcss -i ./static/src/app.css -o ./static/vendor/tailwind.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
